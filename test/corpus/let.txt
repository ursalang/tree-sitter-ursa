==============
Let statements
==============

let a = x;
let fn f(x) { x + 1 };

---

(module (sequence
    (let (identifier) (identifier))
    (let (identifier) (lambda (identifier)
        (sequence (binary_exp (identifier) (number)))))))
