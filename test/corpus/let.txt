==============
Let statements
==============

let a = x;
let fn f(x) { x + 1 };

---

(module (sequence
    (let (ident) (ident))
    (let (ident) (lambda (ident)
        (sequence (binary_exp (ident) (number)))))))
